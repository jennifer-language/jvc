# The Jennifer interpreter binary (copied in locally, not source).
/jennifer
/jennifer-tiny

# Crash-atomic flatdb temp files.
*.tmp

# CLI install artifacts.
/decks/
camcorder.lock

# Editor / OS noise.
*.swp
.DS_Store

# makepkg build artifacts, when building the PKGBUILD in place.
/packaging/archlinux/src/
/packaging/archlinux/pkg/
/packaging/archlinux/*.pkg.tar.*
/packaging/archlinux/*.log
